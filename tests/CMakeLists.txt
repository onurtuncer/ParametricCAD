# tests/CMakeLists.txt
# Add a new test group by creating a subdirectory with its own CMakeLists.txt
# and adding an add_subdirectory() call here.

add_subdirectory(geometry)
add_subdirectory(io)
add_subdirectory(math)
